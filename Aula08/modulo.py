import os
def soma(a, b):
    return a + b

def sub(a,b):
    return a - b

def multiplicacao(a,b):
    return a * b

def divisao(a,b):
        return a / b
if any == 0:
         "Não é possível dividir por zero."

def limpar_terminal():
    os.system("cls" if os.name == "nt" else "clear")